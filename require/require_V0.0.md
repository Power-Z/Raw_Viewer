# V0.0

# V0.1

1、基于 E:\code\Raw_viewer\require\Raw_Viewer.md 中的软件需求，给出一个架构设计和实施方案，需要针对相关问题提出解决
2、后续基于该架构和实施方案分三步实现raw view的初步功能
3、我需要你帮我写好 AGENTS.md,以及长期维护中需要的记录文件等

# V0.2

1、目前的raw图加载存在问题，测试raw图为 E:\code\Raw_viewer\Data\Test_data\B0012535.B0011072.raw，以11904*8842 16bit RGG加载该raw图后，需要显示的bayer pattern，对应.raw\.RAW\.bin\.BIN格式的文件，你按照skip后顺序展开数据即可，不需要其他处理
2、左侧双击加载raw图后需要一个加载动画

# v0.2.1 问题修复

1、以 E:\code\Raw_viewer\Data\Test_data\B0012535.B0011072.raw 为例，我按照11776*8842 uint16bit 小端对齐打开后，我不需要RGB的渲染，你直接显示bayer channel灰度即可，.raw\.RAW\.bin\.BIN格式的文件打开都显示灰度值即可

# V0.3 raw图像素统计功能

1、工具栏中添加一个 ”像素统计“（翻译为合适的英文）功能，该功能只针对原始bayer raw进行处理

2、点击该功能后，跳出一个子窗口，该子窗口中分为三个部分，顶部为功能选择，有：status、水平box、垂直box、Line、WB 五个功能；中间为显示结果控制区，主要用于控制底部显示结果；底部为结果显示区域；三者之间的比例为1：2：5
3、status、水平box、垂直box点击后出现一个箭头，在raw图上点击左键开始框选，出现一个左上角固定，右下角跟随鼠标的矩形框，在此点击左键则完成区域框选，status为显示框选区域的均值、最大值、最小值、std、像素数量，同时还需要显示框选区域的灰度直方图统计结果；水平box 为将框选区域水平平均后统计的均值、最大值、最小值、std，同时需要显示水平平均后得到的折线图；垂直box同理。Line则是左键为一个line的起始，再次点击则结束，同时统计均值、最大值、最小值、std、像素数量和Line上的灰度统计（x轴为line的长度）。WB功能目前暂时空出
4、你需要仔细分析性能实现和需要设计一个高级感的UI给子窗口

# V0.4 bayer channel Extract 工具功能优化

1、Bayer Extract点击后出现的子窗口显示修改为：
（1）出现一个n*m的可选与可需求小矩阵阵列，选择的小矩阵显示颜色，未选择的则显示灰色，最顶部是下拉框，顶部的下拉框预置 2*2、4*4、8*8 三种pattern，可以自己预设和命名新的pattern；下拉框右侧为选择行优先或列优先。
（2）基于选择的n*m区域，作为对全图操作的最小可重复单元，图像尺寸（长和宽）不满足整数倍最小可重复单元时，需要跳出警示框提示，若用户点击确认，则最边缘的区域也进行操作（只操作最后一个最小可重复单元能够覆盖的区域。曹
（3）操作的逻辑为：将全图划分为一个个的最小可重复单元，对于每一个最小可重复单元内，小矩阵阵列对应选择的点则保留，未点击的点则不保留，并且按照行优先（即所有可选点向上补齐空位，即若一个最小可重复单元内，一个像素的上方（y-1）的位置没有点被选择，则其上移一位，直到上移有像素被选择，按照从上向下的操作顺序）或列优先（类似行优先逻辑）进行像素重排列，然后得到全图的结果o
2、需要可以保存、加载、命名这一个子窗口的配置
3、UI设计的简洁、现代一些
4、小矩形阵列的选择和取消选择UI也需要高级感


# V0.5 增加 recent files功能

1、在文件菜单中加入 最近打开的文档（翻译为英文，可能是Recent files）选项，鼠标点击或放上去后，会显示最近是个打开的文档，点击后按照保存的配置加载

2、需要考虑文件已经删除

# V0.6 bayer channel Extract 工具

1、bayer channel Extract工具的窗口中删除 use full image的选项及其配置，默认就是对全图进行提取

2、删除"2x2 mask 1 select……"等字样，UI内容减少一些

3、无论如何操作，都是基于原图，比如我提取了一次2*2的左上角，然后又提取了一次右上角，第一次基于全图提取，第二次还是基于原图

4、UI整体缩小，并且随着选择的block排列数量增大或缩小整体UI

5、删除小矩阵阵列中每一个小矩阵上的文字标注，在矩阵顶部和左侧标志坐标需要即可，不需要内部标注每一个小矩阵

6、不需要export SCV选项

7、pattern选项中光标在4x4则4x4选项全x白，修复这个问题，同时缩小这个pattern框的宽度，能够显示8x8等字样即可；右侧的紧凑类型也是这个问题

8、删除 1/16 selected选项部分的显示

9、UI整体缩小


## V0.6.1 bayer channel Extract优化1

1、现在可以做到pattern从数量少到数量多的时候整个子窗口变大，但是反过来UI不会变小（指的是整个子窗口的框），修复这一问题

2、为什么我选择所有的小矩形（无论2x2还是4x4、8x8）再点击extract后，结果不是原图，是你的什么处理有问题吗

3、将extract_display改名为Extract即可

4、现在的提取过程比较慢，是否有什么优化方法，你告知我做决策


## V0.6.2 bayer channel Extract优化2

1、小矩阵阵列占UI的比重大一些，现在垂直方向只占大约四分之一，提高奥五分之二

2、pattern&packing 字样简化为Pattern，并且该区域任何缩放下都维持1.5倍字体的行高即可，不跟随UI缩放，现在的UI 子窗口缩放后这个区域也会跟着缩放，不符合预期；pattern选择栏下面一行的自定义pattern默认时候不显示，再pattern多选框中加入一项：自定义，选择后再出现自定义相关的框；

3、小矩形块不使用圆角矩形，直接使用正方形即可，并且小矩形之间距离缩小

4、向上紧凑选框部分，不需要这么复杂的选项，你保留英文即可，行优先、列优先，并且在这个选项框右侧加一个问好，点击后可以解释这个选项的意思

5、这个工具的子窗口UI整体还是需要缩小和简化，宽度和高度都需要再缩小


## V0.6.3 bayer channel Extract优化3

1、问号的边框太大了，使用常规软件中提示的设计

2、加大默认2x2 时候的小正方形大小，比如2x2时候小正方形大小为4，4x4时候3，8x8时候为2这样的比例关系，自定义时候也类似的比例

3、不需要顶部显示”Source 11904x8842"等字样，直接删除

## V0.7

1、像素值标注优化

（1）在raw图的像素值标注中，不需要显示Raw字样，直接显示值即可，并且字体只使用黑、白两色，具体颜色根据背景像素的显示灰度调整，以确保字体显示清晰

（2）显示像素值时默认不在像素边界上加一个线，直接显示像素即可

（3）像素中的字体标志大小高度约为像素显示高度的七分之一，宽度根据具体的数字位数决定

2、目前像素值标注后raw图显示、拖拽、缩放不是很流畅，分析原因并优化

## V0.7.1

1、现在的像素值标注随着缩放或者移动，会有闪烁，我需要完全跟随像素并且不闪烁（我查看你的输出，是拖拽期间暂停标签查询和绘制，这个不需要，一致标注就行）

2、像素值显示可能会出现遗漏，有的像素并未被标注，缩放后又标注了

3、pixel info窗口调整，不需要现在这些的显示，保留

（1）像素值标注(包括raw图下的像素值标注和RGB图的标注，根据显示的图像类型自动判断）

（2）Bayer mesh（即根据bayer pattern给上特定的RGGB 颜色遮罩，GR需要和Gb使用不同的绿色遮罩）

（3）Bayer pattern（在像素的右下角标注一个像素边长七分之一的英文，R\Gr\Gb\B 四个字样）

3、像素值标注的文字大小改为六分之一高


## V0.7.2 像素标注优化

1、现在初始状态下放大像素值并不会又像素值，需要点pixel info工具后才会有，修复该问题

2、bayer pattern的标注，不同channel使用不同颜色的字体（Gr和Gb需要区分开）

3、现在从无像素值标注到有像素值标注还是会有卡顿，优化性能表现，增加流畅度


## V0.7.3 Pixel statistics 优化1

1、Pixel statistics中的功能都是基于显示的raw图进行，就如一开始建议的，数据分为原始数据、Pipe数据以及显示数据，比如使用bayer extract工具提取得到了一个结果，再点开统计功能，是对这个处理得到的结果进行统计

2、统计区域的选择逻辑修改为，点击并保持左键则为左上角起始点，此时拖拽鼠标框选区域，完成后松开左键则为统计区域的右下角

3、像素统计功能的窗口的UI使用专业风格，类似，matlab以及maxlm的分格，现在分格像是一个日常软件，UI整体排布较为混乱也不紧凑，也不需要众多的说明文字，Ui整体宽度缩小一些

## V0.7.4 Pixel statistics 优化2

1、channel功能设置为一个可选项，而不是现在的下拉栏，选择该项目后，底部的结果会显示四个，分别为bayer channel的四个channel

2、取消Line width设置，默认设置为1，将该项的设置放到菜单中，偏好 菜单下设置 Pixel statistics偏好设置，其中包括线宽、Histogram Bins、Datapoints等，保证UI的简洁



## V0.7.5 像素标注优化2

1、像素标注也是类似Pixel statistics功能，基于显示的raw图进行，无论我进行了何种操作，都标注当前显示的raw图像素值，现在当我extract像素后，再使用像素值标注存在问题，修复该问题，也可能是显示问题？


## V0.7.6 综合优化

1、raw图操作区域增加按住鼠标中键能够拖拽raw图，此操作主要用于再tool中，左键被占用的情况

2、在raw图显示区域的顶部和底部加上类似photoshop的刻度线，刻度线的数值需要和像素位置严格对齐，不要有偏差

3、raw图显示区域右上角加入一个缩略图指示，用于指示当前显示区域在全图的位置，半透明放置在右上角，全图固定不变，当前显示的区域大小和位置以一个有颜色的矩形框标注在示意图中

4、raw图显示区域的底部和右侧增加滚轮，可以拖拽调整raw图显示区域

5、默认不标注Bayer pattern在像素上，显示像素值即可


## V0.7.7 Filter工具

1、工具区域增加一个Filter，滤波工具，然后加入常用的图像滤波，比如中值滤波、高斯滤波、均值滤波等，该工具也需要一个专业的子窗口，能够选择滤波类型和kernel大小

2、Filter工具基于的当前显示在raw图区域的图像进行，不是针对原始图，需要你帮我仔细优化图像滤波的性能


## V0.7.8 Demosaic 算法调研

1、工具区增加一个 Demosaic 工具，你需要调研常用的 Demosaic 算法，并整理资料在Docs中

2、基于整理的资料实现最常用的三个 Demosaic 算法，加在 Demosaic 工中

3、Demosaic工具也需要一个子窗口，窗口由你来设计，需要专业紧凑
